import { styled } from "@mui/system";

export const ContentMain = styled("main")(({ theme }) => ({
  width: "100%",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  padding: "40px 0",
}));
